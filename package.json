{
  "name": "root",
  "private": true,
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged --pattern \"./src/**/*.ts\" --pattern \"./test/**/*.ts\""
    }
  },
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.build.json",
    "clean:package": "node scripts/clean.js"
  },
  "devDependencies": {
    "@types/benchmark": "^1.0.31",
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "benchmark": "^2.1.4",
    "husky": "^1.3.1",
    "jest": "^24.7.1",
    "microtime": "^3.0.0",
    "prettier": "^1.17.0",
    "pretty-quick": "^1.10.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.3"
  }
}
